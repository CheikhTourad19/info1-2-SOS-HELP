{% extends 'base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-50">
        <div class="max-w-md w-full p-6 bg-white rounded-lg shadow-md">
            <div class="text-center">
                <svg class="mx-auto h-16 w-16 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                <h1 class="mt-3 text-2xl font-bold text-gray-800">Email envoyé avec succès</h1>
            </div>

            <div class="mt-4 space-y-4 text-gray-600">
                <p>
                    Un email contenant un lien pour réinitialiser votre mot de passe a été envoyé à l'adresse indiquée.
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>
                    Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou 
                    <a class="text-indigo-600 hover:text-indigo-500" href="{{ path('app_forgot_password_request') }}">essayez de nouveau</a>.
                </p>
            </div>

            <div class="mt-8 text-center">
                <a href="{{ path('app_login') }}" class="text-sm text-indigo-600 hover:text-indigo-500">
                    Retour à la connexion
                </a>
            </div>
        </div>
    </div>
{% endblock %}